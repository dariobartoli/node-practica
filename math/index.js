export function add(x, y) {
  return x + y;
}
export function subtract(x, y) {
  return x - y;
}
export function multiply(x, y) {
  return x * y;
}
export function divide(x, y) {
  return x / y;
}

/* module.exports = {
    add,
    subtract,
    multiply,
    divide
} */

/* export default {
  add,
  subtract,
  multiply,
  divide
}; */

//ahora puedo exportar con export y con import, despues de instalar "Type = module" en el package json
